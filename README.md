# LegacyBuilt Command OS™ — v3.2.9

**Hotfix:** Tabs not switching — restored the tab router with click + hash routing and ARIA states.  
All v3.2.8 features remain: planner autosave, leads→calendar sync, caption & post saving, brand fill‑in‑the‑blank, export/import, etc.

**Deploy**: Upload `index.html`, `styles.css`, `app.js`, `README.md` to repo root and enable GitHub Pages.
